# Moved to https://zariffidachowdhury.github.io/

This repository used to hold my site at `zariffidachowdhury.github.io/portfolio/`. The site now lives at the user-pages address above, in the [`zariffidachowdhury.github.io`](https://github.com/zariffidachowdhury/zariffidachowdhury.github.io) repository.

`index.html` here is a redirect (a zero-second `meta refresh` plus a canonical link), so old links keep working. The repository stays public for that reason; deleting it would break the old address.

If anything still points at `zariffidachowdhury.github.io/portfolio/resume.pdf`, update the link to `https://zariffidachowdhury.github.io/resume.pdf`.
